#spring:
#  config:
#    import: optional:configserver:http://localhost:8888
#  application:
#  name: discovery-service

spring:
  application:
    name: discovery-service
  config:
    import: optional:configserver:http://localhost:8888
